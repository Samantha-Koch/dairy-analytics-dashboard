# Backend

This folder contains the API service, background jobs, data access, and analytics/transform code used by the dashboard.

## Quickstart

Install deps:

```bash
pip install -r requirements.txt
```

Run the API (dev):

```bash
uvicorn backend.app.main:app --reload
```

